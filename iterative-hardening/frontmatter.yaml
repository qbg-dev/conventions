name: iterative-hardening
description: >
  Iterative test-and-harden loop for agent harnesses (chatbots, tool-use agents,
  pipelines). Covers: adversarial test generation, harness fixing, regression
  sampling, and test suite pruning. Read when hardening any agent harness against
  its test suite, or when running red-team → fix → prune cycles.
type: convention
version: "1.0"
owner: warren
files:
  convention: CONVENTION.md
